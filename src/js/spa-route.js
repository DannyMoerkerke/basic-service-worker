export const template = `
  <div>
    <h1>SPA route</h1>
    <p>
      This is an SPA route (Single Page App) that doesn't trigger a page refresh by default but that does in a full 
      page reload when there is a new service worker waiting to be activated.
    </p>
    <p>
      <a href="/">Home</a>
    </p>
  </div>
`
